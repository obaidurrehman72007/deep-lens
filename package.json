{
  "name": "full-stack-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/server.js",
    "build": "npm install && cd api && npm install && cd .. && cd Frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.0",
    "mongoose": "^8.0.0",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5"
  }
}